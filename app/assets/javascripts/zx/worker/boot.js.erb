goog.require('zx');

goog.require('zx.Worker');

(function(top) {
    var zx = top['zxWorker'];

    if (zx._started) {
        return;
    }

    console.log('worker starting');
    zx._started = true;
    zx._worker = new zx.Worker(top, '<%= ApplicationHelper.base_uri %>');
})(this);
